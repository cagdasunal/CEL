"""CEL transactional email (Mailgun). Import from the submodule:

    from tools.mailer.send import send_email, EmailError

CLI:  python3 -m tools.mailer.send --to a@b.com --subject Hi --text Body
"""
