# Logs
logs/
run_log.txt

# Secrets (tracker-091 — GEMINI_API_KEY etc.)
.env

# Python
__pycache__/
*.pyc
venv/
.venv/
.venv313/

# Node
node_modules/

# OS
.DS_Store

# Auto-generated per-run diagnostic (regenerated every workflow run by
# tools/check_sitemap_urls.py; tracking it caused the "Update Sitemap
# & LLMs.txt" workflow to fail with "cannot pull with rebase: You have
# unstaged changes" — see run 24472895594, 2026-04-15).
data/sitemap-check-report.json

# Arabic visual-pass screenshots — large PNGs uploaded as a CI artifact for review,
# not committed (the generated *.css + *.report.md + state.json ARE committed).
data/arabic-visual/screenshots/

# SEO-intel per-run logs + ad-hoc run output dirs (audit 112 M2, 2026-05-25).
# Translation/internal-linking/summary scratch runs (~21 MB) that must NOT be
# swept into a commit by an accidental `git add data/`. The committed
# indexing-audit/*.json snapshots are .json (not matched) and stay tracked.
data/seo-intel/*.log
data/seo-intel/link-blogs-*/
data/seo-intel/run-*/
data/seo-intel/tx-*/
data/seo-intel/translate-dryrun-*/
data/seo-intel/translate-live-*/
data/seo-intel/summary-dryrun/
